# Kwartetmaker op gewone webhosting (Apache / LiteSpeed).
Options -Indexes

# Bestanden die alleen bij de lokale versie horen: niet uitserveren.
<FilesMatch "\.(py|sh|command|md)$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>
